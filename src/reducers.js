const reducerTypes = {
  PLAYER_MOVE: 'PLAYER_MOVE'
}

const reducers = (state, action) => {
  switch (action.type) {
    default:
      return Object.assign({}, state)
  }
}

module.exports = {
  reducerTypes,
  reducers
}
